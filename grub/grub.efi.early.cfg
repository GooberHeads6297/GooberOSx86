# Early config for UEFI GRUB on installed FAT/ESP (eMMC / USB / SSD).
# Search by volume label so we do not depend on (hd0,gpt1) / (hd0,msdos1).
search --no-floppy --set=root --label GOOBEROS
set prefix=($root)/boot/grub
echo "GooberOS ESP found; loading menu..."
configfile $prefix/grub.cfg
